spring:
  datasource:
    url: jdbc:tc:postgresql:17:///booklore_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
app:
  path-config: build/app-data
